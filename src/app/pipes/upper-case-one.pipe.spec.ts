import { UpperCaseOnePipe } from './upper-case-one.pipe';

describe('UpperCaseOnePipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseOnePipe();
    expect(pipe).toBeTruthy();
  });
});
